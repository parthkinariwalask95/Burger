<h1 class="f1"><marquee>Burger</marquee></h1>


{{!-- 
<ul>
  {{#each burger}}
  <li>
      <p>{{id}}</p>
      <p>{{burger_name}}</p> 
       <button data-id="{{this.id}}" class="dvo">Devoure It!!!</button>
  </li> 
   {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
 <div class="form-group">
    <label for="ca">burger_name:</label>
    <input type="text" id="bname" name="name">
  </div><br>
   <div class="form-group">
  <button id="submit1" type="submit">Add burger</button><br>
  </div>
</form> --}}





<div class="container">
  <div class="row">
    <div class="col-md-5">
      {{#each burger}} {{#unless devoured}}
      <li class="f1">{{id}}. {{burger_name}}</li><br>
      <button id="b2" data-id="{{this.id}}" class="dvo">Devoure It!!!</button>
      {{/unless}} {{/each}}
    </div>
    <div class="col-md-2"></div>
    <div class="col-md-5">

      {{#each burger}} {{#if this.devoured}}

      <li class="f1">{{this.id}}. {{this.burger_name}}</li>
      {{/if}} {{/each}}

    </div>
  </div>
</div>
</div>


<div class="container">
  <div class="row">
    <div class="col-md-4">

    </div>
    <div class="col-md-5">
<h3 class="f1" >Add Buger</h3>
<form class="create-form">

  <div class="form-group">
    <label class="f1" for="lebel">Burger Name :</label>
    <br>
    <input type="text" id="bname" name="name">
  </div>
<button type="submit" id="submit1" class="b1">Add Burger</button>
</form>
    </div>
    <div class="col-md-3">

    </div>
  </div>
</div>
</div>
