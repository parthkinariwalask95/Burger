
{{!-- <h1>tag --}}
<h1 class="f1"><marquee>Burger</marquee></h1>

{{!-- container --}}
<div class="container">
  {{!-- row --}}
  <div class="row">
    {{!-- column --}}
    <div class="col-md-5">
      {{#each burger}} {{#unless devoured}}
     {{!-- list of id and burger_name --}}
      <li class="f1">{{id}}. {{burger_name}}</li><br>
      {{!-- button --}}
      <button id="b2" data-id="{{this.id}}" class="dvo">Devoure It!!!</button>
      {{/unless}} {{/each}}
    </div>
    <div class="col-md-2"></div>
    <div class="col-md-5">

      {{#each burger}} {{#if this.devoured}}

      <li class="f1">{{this.id}}. {{this.burger_name}}</li>
      {{/if}} {{/each}}

    </div>
  </div>
</div>
</div>

{{!-- container --}}
<div class="container">
  {{!-- row --}}
  <div class="row">
    {{!-- column --}}
    <div class="col-md-4">

    </div>
    <div class="col-md-5">
<h3 class="f1" >Add Buger</h3>
{{!-- form --}}
<form class="create-form">

  <div class="form-group">
    {{!-- label --}}
    <label class="f1" for="lebel">Burger Name :</label>
    <br>
    {{!-- textbox --}}
    <input type="text" id="bname" name="name">
  </div>
{{!-- button --}}
<button type="submit" id="submit1" class="b1">Add Burger</button>
</form>
    </div>
    <div class="col-md-3">

    </div>
  </div>
</div>
</div>
